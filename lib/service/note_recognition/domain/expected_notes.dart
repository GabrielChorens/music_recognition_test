import 'note.dart';

const C4 = Note(
  lowerBoundFrequency: 258,
  upperBoundFrequency: 265,
  noteName: "C",
);

const D4 = Note(
  lowerBoundFrequency: 288,
  upperBoundFrequency: 302,
  noteName: "D",
);

const E4 = Note(
  lowerBoundFrequency: 325,
  upperBoundFrequency: 334,
  noteName: "E",
);

const F4 = Note(
  lowerBoundFrequency: 345,
  upperBoundFrequency: 356,
  noteName: "F",
);
